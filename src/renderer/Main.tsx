import { Button, Text } from '@chakra-ui/react';
import React from 'react';


const Main = () => {
    return (
        <>
            <Text fontSize={'xl'}>Rest Client using React + Typescript + Electron</Text>
            <Button colorScheme={'facebook'}>Dummy Button</Button>
        </>
    )
}

export default Main